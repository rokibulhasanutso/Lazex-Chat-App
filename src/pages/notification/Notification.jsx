

const Notification = () => {
    const handleClick = () => {


    }

    return (
        <div>
            <button className="p-2 border" onClick={handleClick}>get all users</button>
        </div>
    );
};

export default Notification;